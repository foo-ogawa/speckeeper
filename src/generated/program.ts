// Auto-generated by cli-contracts. Do not edit.
// @ts-nocheck
import { Command } from "commander";
import { commandDefinitions, deriveCommandPolicy } from "./policy.js";

export interface CommandHandlers {
  init: (options: { force?: boolean; format?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  build: (options: { config?: string; output?: string; format?: string; watch?: boolean; verbose?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  lint: (options: { config?: string; phase?: string; strict?: boolean; fix?: boolean; format?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  drift: (options: { config?: string; update?: boolean; format?: string; failOnDrift?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  check: (type: string | undefined, options: { config?: string; strict?: boolean; verbose?: boolean; coverage?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  new: (type: string | undefined, options: { kind?: string; name?: string; output?: string; template?: string; dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  impact: (id: string | undefined, options: { config?: string; depth?: string; direction?: string; format?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  scaffold: (options: { source?: string; output?: string; force?: boolean; dryRun?: boolean; format?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  convert: (file: string | undefined, options: { output?: string; dryRun?: boolean }, parentOpts: Record<string, unknown>) => Promise<void>;
  auditRequirements: (options: { config?: string; adapter?: string; model?: string; dryRun?: boolean; showPrompt?: boolean; failOn?: string; output?: string; reportFormat?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  proposeTraceLinks: (options: { config?: string; adapter?: string; model?: string; dryRun?: boolean; showPrompt?: boolean; failOn?: string; output?: string; reportFormat?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  explainImpact: (options: { adapter?: string; model?: string; dryRun?: boolean; showPrompt?: boolean; failOn?: string; output?: string; reportFormat?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
  proposeAcceptanceCriteria: (specIds: string[], options: { config?: string; adapter?: string; model?: string; dryRun?: boolean; showPrompt?: boolean; failOn?: string; output?: string; reportFormat?: string }, parentOpts: Record<string, unknown>) => Promise<void>;
}

export function createProgram(
  handlers: CommandHandlers,
  version: string,
): Command {
  const program = new Command();
  program
    .name("speckeeper")
    .version(version, "-V, --version")
    .description("Requirements and design management framework with TypeScript DSL.");

  program.option("--introspect", "Output derived policy as JSON without executing the command");

  program
    .command("init")
    .description("Initialize a new speckeeper project with starter templates.")
    .option("-F, --force", "Overwrite existing files.", false)
    .option("--format <format>", "Spec data format: ts (default) or yaml.", "ts")
    .action(async (opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("init", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.init(opts, globalOpts);
    });

  program
    .command("build")
    .description("Generate docs/ and specs/ from TypeScript models.")
    .option("-c, --config <path>", "Path to config file.")
    .option("-o, --output <path>", "Base output directory path.", ".")
    .option("-f, --format <format>", "Output format: markdown, json, both.", "both")
    .option("-w, --watch", "Watch for changes and auto-regenerate.", false)
    .option("-v, --verbose", "Show detailed output.", false)
    .action(async (opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("build", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.build(opts, globalOpts);
    });

  program
    .command("lint")
    .description("Check design integrity (ID duplicates, references, layer violations, etc.).")
    .option("-c, --config <path>", "Path to config file.")
    .option("-p, --phase <phase>", "Phase gate to check against: REQ, HLD, LLD, OPS.")
    .option("-s, --strict", "Treat warnings as errors.", false)
    .option("--fix", "Attempt to fix auto-fixable issues (not yet implemented).", false)
    .option("-f, --format <format>", "Output format: text, json, github.", "text")
    .action(async (opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("lint", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.lint(opts, globalOpts);
    });

  program
    .command("drift")
    .description("Check if generated files have been manually edited.")
    .option("-c, --config <path>", "Path to config file.")
    .option("-u, --update", "Auto-update if differences are found (not yet implemented).", false)
    .option("-f, --format <format>", "Output format: text, json, diff.", "text")
    .option("--fail-on-drift", "Exit with code 1 if drift is detected (for CI).", false)
    .action(async (opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("drift", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.drift(opts, globalOpts);
    });

  program
    .command("check")
    .description("Check external SSOT conformance (including custom models).")
    .argument("[type]", "Type of check to run. Filters sources by type.")
    .option("-c, --config <path>", "Path to config file.")
    .option("--strict", "Treat warnings as errors.", false)
    .option("-v, --verbose", "Show detailed output.", false)
    .option("--coverage", "Check if all testable acceptance criteria are covered by TestRefs.", false)
    .action(async (type, opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("check", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.check(type, opts, globalOpts);
    });

  program
    .command("new")
    .description("Create a new element with auto-generated ID.")
    .argument("<type>", "Element type to create.")
    .option("-k, --kind <kind>", "Sub-kind (e.g. functional, non-functional for requirements).")
    .option("-n, --name <name>", "Name of the element.")
    .option("-o, --output <path>", "Output directory path.")
    .option("-t, --template <path>", "Path to template file.")
    .option("--dry-run", "Preview generated file content without writing.", false)
    .action(async (type, opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("new", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.new(type, opts, globalOpts);
    });

  program
    .command("impact")
    .description("Analyze impact of changes to an ID.")
    .argument("<id>", "Spec ID to analyze (e.g. REQ-001, ENT-ORDER).")
    .option("-c, --config <path>", "Path to config file.")
    .option("-d, --depth <n>", "Analysis depth (reference tracking level).", "3")
    .option("--direction <direction>", "Analysis direction: upstream, downstream, both.", "both")
    .option("-f, --format <format>", "Output format: text, json, mermaid.", "text")
    .action(async (id, opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("impact", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.impact(id, opts, globalOpts);
    });

  program
    .command("scaffold")
    .description("Generate _models/ from a Mermaid flowchart definition.")
    .option("-s, --source <path>", "Path to Markdown file containing Mermaid flowchart.")
    .option("-o, --output <path>", "Output directory.", "design/")
    .option("-F, --force", "Overwrite existing files.", false)
    .option("--dry-run", "Preview generated files without writing.", false)
    .option("--format <format>", "Spec data format: ts (default) or yaml.", "ts")
    .action(async (opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("scaffold", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.scaffold(opts, globalOpts);
    });

  program
    .command("convert")
    .description("Convert a TS spec data file to YAML format.")
    .argument("<file>", "Path to TS spec data file.")
    .option("-o, --output <path>", "Output file path (default: same name with .yaml extension).")
    .option("-n, --dry-run", "Preview conversion without writing.", false)
    .action(async (file, opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("convert", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.convert(file, opts, globalOpts);
    });

  program
    .command("audit-requirements")
    .description("Run LLM-based requirement quality audit.")
    .option("-c, --config <path>", "Path to config file.")
    .option("-a, --adapter <name>", "SDK adapter to use for LLM execution.")
    .option("--model <name>", "LLM model override.")
    .option("-n, --dry-run", "Output the constructed prompt without calling LLM.", false)
    .option("--show-prompt", "Display the constructed LLM prompt on stderr.", false)
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the audit report.", "json")
    .action(async (opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("audit-requirements", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.auditRequirements(opts, globalOpts);
    });

  program
    .command("propose-trace-links")
    .description("LLM-based traceability link proposal.")
    .option("-c, --config <path>", "Path to config file.")
    .option("-a, --adapter <name>", "SDK adapter to use for LLM execution.")
    .option("--model <name>", "LLM model override.")
    .option("-n, --dry-run", "Output the constructed prompt without calling LLM.", false)
    .option("--show-prompt", "Display the constructed LLM prompt on stderr.", false)
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the report.", "json")
    .action(async (opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("propose-trace-links", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.proposeTraceLinks(opts, globalOpts);
    });

  program
    .command("explain-impact")
    .description("LLM-based explanation of impact analysis output.")
    .option("-a, --adapter <name>", "SDK adapter to use for LLM execution.")
    .option("--model <name>", "LLM model override.")
    .option("-n, --dry-run", "Output the constructed prompt without calling LLM.", false)
    .option("--show-prompt", "Display the constructed LLM prompt on stderr.", false)
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the report.", "json")
    .action(async (opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("explain-impact", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.explainImpact(opts, globalOpts);
    });

  program
    .command("propose-acceptance-criteria")
    .description("LLM-based acceptance criteria proposal.")
    .argument("[specIds...]", "Spec IDs to propose criteria for. Defaults to all specs if omitted.")
    .option("-c, --config <path>", "Path to config file.")
    .option("-a, --adapter <name>", "SDK adapter to use for LLM execution.")
    .option("--model <name>", "LLM model override.")
    .option("-n, --dry-run", "Output the constructed prompt without calling LLM.", false)
    .option("--show-prompt", "Display the constructed LLM prompt on stderr.", false)
    .option("--fail-on <level>", "Minimum severity that causes a non-zero exit.", "error")
    .option("-o, --output <file>", "Write result to a file instead of stdout.")
    .option("--report-format <fmt>", "Output format for the report.", "json")
    .action(async (specIds, opts, cmd) => {
      const globalOpts = cmd.optsWithGlobals();
      if (globalOpts.introspect) {
        const policy = deriveCommandPolicy("propose-acceptance-criteria", opts);
        console.log(JSON.stringify(policy, null, 2));
        return;
      }
      await handlers.proposeAcceptanceCriteria(specIds, opts, globalOpts);
    });

  return program;
}
